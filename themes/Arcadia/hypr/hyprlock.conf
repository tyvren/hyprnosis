source = ~/.config/hypr/theme.conf

$font = JetBrainsMono Nerd Font

# GENERAL
general {
  hide_cursor = true
}

# BACKGROUND
background {
  path = $HOME/.config/hyprnosis/wallpapers/arcadia
  blur_passes = 1
}

# TIME
label {
  text = $TIME12
  color = $ash
  font_size = 90
  font_family = $font
  position = -30, 0
  halign = center
  valign = top
}

# DATE
label {
  text = cmd[update:43200000] date +"%A, %d %B %Y"
  color = $ash
  font_size = 25
  font_family = $font
  position = -30, -150
  halign = center
  valign = top
}

# USER AVATAR
image {
  path = $HOME/.face
  size = 100
  border_color = $granite
  position = 0, 75
  halign = center
  valign = center
}

# INPUT FIELD
input-field {
  size = 300, 60
  outline_thickness = 4
  dots_size = 0.2
  dots_spacing = 0.2
  dots_center = true
  outer_color = $crimson
  inner_color = $ash
  font_color = $crimson
  fade_on_empty = false
  hide_input = false
  check_color = $crimson
  fail_color = $granite
  fail_text = <i>$FAIL <b>($ATTEMPTS)</b></i>
  capslock_color = $crimson
  position = 0, -47
  halign = center
  valign = center
}
